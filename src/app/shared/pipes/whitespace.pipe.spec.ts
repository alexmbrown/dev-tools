import { WhitespacePipe } from './whitespace.pipe';

describe('WhitespacePipe', () => {
  it('create an instance', () => {
    const pipe = new WhitespacePipe();
    expect(pipe).toBeTruthy();
  });
});
